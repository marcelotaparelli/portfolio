---
interface Props {
  direction?: 'right' | 'diagonal' | 'left';
}
const { direction = 'right' } = Astro.props;
---

<svg
  class:list={['arrow', { 'arrow--left': direction === 'left' }]}
  width="20"
  height="20"
  viewBox="0 0 24 24"
  fill="none"
  aria-hidden="true"
>
  {direction === 'diagonal' ? (
    <path d="M6 18 18 6M6 6h12v12" />
  ) : (
    <path d="M4 12h15m-6-6 6 6-6 6" />
  )}
</svg>
