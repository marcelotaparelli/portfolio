---
translationKey: salus
locale: pt-BR
slug: salus
title: Salus
description: API backend em TypeScript para gestão de pacientes, emissão de JWT e regras de domínio, com arquitetura em camadas, PostgreSQL + Prisma e 47 testes automatizados.
status: published
reviewed: true
order: 4
category: Backend · API
role: Desenho e implementação da API
context: Projeto público autoral — código aberto
technologies:
  [
    TypeScript,
    Node.js,
    Express,
    PostgreSQL,
    Prisma,
    Vitest,
    Docker,
    GitHub Actions,
  ]
externalUrl: https://github.com/marcelotaparelli/salus
contribution: 'API autoral: domínio, login com emissão de JWT, persistência com Prisma e 47 testes; evolução atual voltada a autorização e limites operacionais.'
---

## Problema

Exercícios de backend frequentemente param no CRUD feliz: sem regras de
domínio consistentes, sem persistência bem isolada, sem autenticação real e
sem testes suficientes para evitar regressões.

O Salus trabalha um problema pequeno mas realista: gestão de pacientes com
regras de domínio e autenticação de usuários. O repositório é público, então
cada decisão descrita aqui pode ser inspecionada no código.

## Decisões de engenharia

- Arquitetura em camadas, com as dependências apontando para o domínio.
- Domínio independente de Express e Prisma; os casos de uso não conhecem o
  Prisma.
- Contratos definidos por repository interfaces; Prisma isolado na camada
  de infraestrutura.
- Casos de uso dependem de ports/interfaces (repositórios, PasswordHasher,
  TokenGenerator), não de implementações de infraestrutura; doubles em
  memória permitem testar a aplicação sem banco.
- Composição via factories, com injeção explícita de dependências.
- Código explícito e legível: entidades protegem seus próprios invariantes
  e erros de domínio são mapeados para HTTP sem vazar stack (princípios de
  Clean Code).
- PostgreSQL como persistência e Docker Compose para o ambiente local.
- Senhas com Argon2 + pepper server-side; JWT com secret vindo do ambiente,
  validado com Zod no boot.
- Tratamento global de erros: erros inesperados não vazam stack ao cliente.

## Implementação

O fluxo conceitual da API:

**HTTP → Routes / Controllers → Application / Use Cases → Domain →
Repository abstraction → Prisma adapter → PostgreSQL.**

As entidades protegem invariantes como nome, CPF com dígitos verificadores,
telefone e data de nascimento. A infraestrutura implementa os contratos
definidos pelas camadas internas, sem que o domínio conheça detalhes de
framework ou de banco.

## Qualidade

47 testes automatizados — 36 unitários e 11 de integração — cobrindo
domínio, use cases, adapters, HTTP e PostgreSQL real nos testes de
integração. Os testes protegem regras de domínio e decisões da aplicação
contra regressões.

O desenvolvimento do Salus foi conduzido com TDD, usando os testes para
orientar a implementação e evolução das regras de domínio e dos casos de
uso. A implementação foi feita manualmente, sem geração de código por IA.

A fase atual de hardening usa desenvolvimento assistido por agente com revisão
humana. Ela não altera a origem manual do core descrito acima.

O CI executa, em sequência:

**typecheck → lint → unit tests → build → integration tests.**

## Segurança

O que o projeto garante hoje:

- Hash de senhas com Argon2 + pepper server-side; hash nunca retornado
  nas respostas.
- JWT assinado com secret vindo do ambiente; variáveis validadas no boot.
- Arquivo `.env` fora do Git.
- Erros inesperados respondidos sem expor stack ao cliente.

## Evolução em curso

A revisão atual examina verificação do JWT nas rotas, autorização por objeto
e ownership de pacientes, validação das requisições HTTP, erros tipados e
testes de regressão para acesso entre usuários. O cenário em que B tenta ler
um paciente de A e recebe 404 é uma propriedade desejada, ainda não uma
garantia publicada.

Também estão no escopo da revisão paginação e limites de recursos, logs
seguros, correlação de requisições, health/readiness e shutdown gracioso.
A listagem atual usa `findMany()` sem paginação. Não há benchmark de latência;
por isso este case não atribui ganhos de performance à API.

## Trade-offs

- Modular monolith em vez de microservices: um único deploy, sem o custo
  de rede e observabilidade distribuída para este tamanho de problema.
- As repository interfaces adicionam uma pequena camada de mapping, mas
  isolam o Prisma e deixam os casos de uso testáveis sem banco.
- JWT stateless simplifica a infraestrutura, mas não há revogação nem
  refresh.
- Invariantes validadas no domínio; os endpoints HTTP ainda não têm
  schemas Zod dedicados.

## Limitações

Consciência arquitetural, de forma direta: o login emite JWT, mas nenhuma
rota valida esse token atualmente — portanto nenhuma rota é descrita aqui
como protegida. Também não há RBAC, refresh-token rotation, paginação,
rate limiting nem observabilidade estruturada comprovados neste case.
